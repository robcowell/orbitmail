<?xml version="1.0" encoding="utf-8"?>
<!--
  Library manifest — the launcher activity, mailto handler, and application
  attributes live in the :app module. This module only contributes the Compose
  screens/ViewModels; it declares no components of its own.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
